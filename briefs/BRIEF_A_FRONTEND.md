# Brief для агента — Human A (фронтенд + чат-UX)

Скопируй весь текст ниже в свою сессию агента (в папке репозитория PromptBeats).

---

Ты — инженер на хакатон-проекте **PromptBeats**. Работаешь в клонированном репозитории
`github.com/dimyard/PromptBeats`. Твоя роль — **Human A: веб-морда и чат-UX**.

## Что за проект
Пользователь в чате пишет пожелание («спокойный лоу-фай бит, 75 BPM») → бэкенд через LLM возвращает **Song JSON** →
браузерный движок Tone.js играет его. Правки — тем же чатом: пишем правку, получаем новый JSON, плеер перезагружается.
Твоя часть — интерфейс: чат, история, кнопки, визуализация трека.

## Сначала прочитай (обязательно)
1. `CONTRACTS.md` — источник правды. Особенно «Контракт 2 — HTTP API» (что зовёшь у бэка) и «Контракт 3 — Player»
   (как зовёшь движок).
2. `sample-song.json` — форма данных, на которой можно работать без бэка.
3. `AGENTS.md` — правила (границы, логирование).
4. `frontend/README.md`, `frontend/src/App.jsx`, `frontend/src/api.js` — базовый UI уже написан, изучи.

## Границы (важно)
Твоя зона — `frontend/` **кроме** `frontend/src/player/` (это Human C) и без бэкенда (Human B). Ты **потребитель**
их интерфейсов: зовёшь бэк через `api.js`, движок — через `createPlayer()`. Их код не переписывай.
Song JSON / HTTP / Player-интерфейс не меняй без правки `CONTRACTS.md` и записи в журнал.

## Что уже готово (рабочий базовый UI)
- `src/App.jsx` — чат (список сообщений + инпут), кнопка Play/Stop, простой грид дорожек, плейхед; всё связано:
  `compose(prompt, currentSong)` → `player.load(song)` → play. Правка = тот же вызов с текущим song.
- `src/api.js` — клиент `compose()` и `getCatalog()`.
- `src/player/…` — движок C (используешь как чёрный ящик по контракту).

## Твоя задача (по приоритету)
1. **Чат и состояние.** Аккуратная история сообщений (пользователь/ассистент/ошибка), автоскролл, индикатор загрузки
   во время запроса, блокировка повторной отправки. Хранить `currentSong` и слать его на правках (уже заложено — доведи).
2. **Обработка ошибок.** Ошибку от бэка (`error.message`) показывать понятным тостом/сообщением, не роняя UI.
   Слушать событие `error` плеера (payload `{code,message,details?}`) и мягко сообщать (напр. «звук X недоступен»).
3. **Визуализация трека.** Сделать грид дорожек читаемым: имя/`role` дорожки, шаги (16 на такт), подсветка активных
   шагов и бегущий плейхед (событие `step` → number). Показывать `title`, `bpm`, `key`.
4. **Ручные контролы дорожек и песни.** Для демо и будущего продукта нужен минимальный control surface поверх Song JSON:
   - mute/unmute дорожки → меняй `track.muted`, затем `player.load(updatedSong)`;
   - level/gain → меняй `track.gain` в диапазоне `0..1`, затем `player.load(updatedSong)`;
   - выбор sound/kit → меняй `track.sound` только на допустимый звук из каталога для текущего `instrument`;
   - step-grid/drum pads → добавляй, удаляй или переключай события в `track.events`;
   - для sampler-дорожек используй Drum note map из `CONTRACTS.md`: `C2=kick`, `D2=snare`, `F#2=closed hat`, ...
   - BPM stepper → меняй `song.bpm` только в диапазоне `40..220`;
   - Bars selector → меняй `song.bars` в диапазоне `1..32`, перестраивай grid на `bars * 16` шагов и перед `load()`
     удаляй события вне нового лупа, а `dur` клампи до его конца;
   - `key` на этом этапе остаётся read-only: простая смена подписи не транспонирует абсолютные `events[].note`.
   Не добавляй live-control методы в Player: ручная настройка в MVP = правка `currentSong` и повторный `load`.
5. **Fallback «Пример».** Кнопка, которая грузит `sample-song.json` напрямую в плеер — страховка на демо, если LLM
   затупит. (Импортируй фикстуру или положи её в `public/`.)
6. **Каталог.** По желанию подтянуть `GET /api/catalog` и показать доступные звуки/роли (чтобы не хардкодить списки).
7. **Полировка.** Приятная тёмная тема, адаптив, понятный первый экран с подсказкой-плейсхолдером.

## Definition of Done
- Ввод пожелания → появляется трек и играет по кнопке Play (первый play — по клику, это требование WebAudio).
- Правка текстом → плеер перезагружается новым Song, история видна.
- Ошибки бэка и плеера не роняют приложение, показываются пользователю.
- Ручные контролы `BPM`, `Bars`, `mute`, `gain` и базовый step-grid меняют `currentSong` и применяются после
  перезагрузки плеера; уменьшение Bars не оставляет событий вне лупа.
- Есть кнопка «Пример» (fallback). Запись в `IMPLEMENTATION_LOG.md` добавлена.

## Контракт вызовов (шпаргалка)
```js
import { compose, getCatalog } from "./api.js";
import { createPlayer } from "./player/index.js";

const player = createPlayer();
const offErr = player.on("error", (e) => toast(e.message)); // on() возвращает unsubscribe!
player.on("step", (s) => setPlayhead(s));                    // s: number

const { song, message } = await compose(prompt, currentSong); // currentSong=null => генерация
await player.load(song);
await player.play(); // по клику

// Ручная настройка дорожки:
const updatedSong = {
  ...currentSong,
  tracks: currentSong.tracks.map((t) =>
    t.id === "drums" ? { ...t, muted: !t.muted, gain: 0.7 } : t
  ),
};
setCurrentSong(updatedSong);
await player.load(updatedSong);
// при размонтировании: offErr(); player.dispose();
```

## Как проверить
Пока бэк не готов — работай на `sample-song.json` (кнопка «Пример» как раз это и делает), UI полностью тестируется без
LLM. Когда B поднимет `:3001` — проверь полный цикл. Проверь в Chrome (минимум) + один другой браузер.

## В конце
Добавь запись в `IMPLEMENTATION_LOG.md` (шаблон там): что сделал, где, как использовать, отклонения, TODO.
Обнови строки статуса компонентов. Коммить маленькими понятными коммитами.
